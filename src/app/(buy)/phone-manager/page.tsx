import PhoneManager from '@/components/PhoneManager';
import React from 'react';

const PhManager = () => {
    return (
        <div>
            <PhoneManager></PhoneManager>
        </div>
    );
};

export default PhManager;